name: Java CI with Checkstyle and JaCoCo

on:
  push:
    branches:
      - master
      - 'feature/**'
  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Set up JDK 25 (Temurin)
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '25'
          cache: 'gradle'

      - name: Build the project
        run: ./gradlew --no-daemon build -x test

      # Запускаем Servlet приложение в фоне
      - name: Start Servlet application
        run: |
          ./gradlew --no-daemon run &
          echo $! > servlet.pid
          sleep 10  # Даем время на запуск

      # Запускаем Spring Boot приложение в фоне
      - name: Start Spring Boot application
        run: |
          ./gradlew --no-daemon bootRun -Dspring.profiles.active=test &
          echo $! > spring.pid
          sleep 20  # Spring Boot обычно стартует дольше

      # Проверяем что серверы запустились
      - name: Wait for servers to be ready
        run: |
          timeout 60 bash -c 'until curl -f http://localhost:8080/leads; do sleep 2; done'
          timeout 60 bash -c 'until curl -f http://localhost:8081/leads; do sleep 2; done'

      - name: Run Checkstyle
        run: ./gradlew --no-daemon checkstyleMain

      - name: Run tests
        run: ./gradlew --no-daemon test

      - name: Generate JaCoCo report
        run: ./gradlew --no-daemon jacocoTestReport

      - name: Verify code coverage
        run: ./gradlew --no-daemon jacocoTestCoverageVerification

      # Останавливаем серверы после тестов
      - name: Stop servers
        if: always()
        run: |
          kill $(cat servlet.pid) || true
          kill $(cat spring.pid) || true
          rm -f servlet.pid spring.pid
