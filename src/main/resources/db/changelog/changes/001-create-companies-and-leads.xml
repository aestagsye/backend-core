<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.25.xsd">

    <changeSet id="001" author="student">
        <!-- Таблица companies -->
        <createTable tableName="companies">
            <column name="id" type="UUID">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="name" type="VARCHAR(255)">
                <constraints nullable="false" unique="true"/>
            </column>
            <column name="industry" type="VARCHAR(100)"/>
        </createTable>

        <!-- Таблица leads -->
        <createTable tableName="leads">
            <column name="id" type="UUID">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="email" type="VARCHAR(255)">
                <constraints nullable="false" unique="true"/>
            </column>
            <column name="status" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>
            <column name="company_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Внешний ключ -->
        <addForeignKeyConstraint
                baseTableName="leads"
                baseColumnNames="company_id"
                constraintName="fk_leads_company"
                referencedTableName="companies"
                referencedColumnNames="id"/>

        <!-- Индексы для производительности -->
        <createIndex tableName="leads" indexName="idx_leads_email">
            <column name="email"/>
        </createIndex>
        <createIndex tableName="leads" indexName="idx_leads_status">
            <column name="status"/>
        </createIndex>
        <createIndex tableName="leads" indexName="idx_leads_company_id">
            <column name="company_id"/>
        </createIndex>
    </changeSet>
</databaseChangeLog>