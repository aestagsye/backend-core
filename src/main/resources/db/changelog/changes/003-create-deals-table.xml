<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.25.xsd">

    <changeSet id="003" author="student">
        <createTable tableName="deals">
            <column name="id" type="UUID">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="lead_id" type="UUID">
                <constraints nullable="false"
                             foreignKeyName="fk_deals_lead_id"
                             references="leads(id)"/>
            </column>
            <column name="amount" type="DECIMAL(15,2)">
                <constraints nullable="false"/>
            </column>
            <column name="status" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <!-- assigned_to удалён, т.к. не используется в сущности -->
        </createTable>

        <createIndex tableName="deals" indexName="idx_deals_lead_id">
            <column name="lead_id"/>
        </createIndex>
        <createIndex tableName="deals" indexName="idx_deals_status">
            <column name="status"/>
        </createIndex>
    </changeSet>
</databaseChangeLog>